# ============================================
# CRITICAL - Application will not start without these
# ============================================

# App Configuration
NEXT_PUBLIC_BASE_URL=https://event.salons-assured.com
NODE_ENV=production
PORT=3000

# CORS Security (optional)
# Set STRICT_CORS=true in production to reject requests without origin header
# Leave unset or false for development/testing (allows Postman, curl)
# STRICT_CORS=true

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/salon-growth-expo?retryWrites=true&w=majority
MONGODB_DB=salon-growth-expo

# Paystack Configuration (Payment Gateway)
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_live_xxxxxxxxxxxxxxxxxxxxx
PAYSTACK_SECRET_KEY=sk_live_xxxxxxxxxxxxxxxxxxxxx

# Brevo/Sendinblue SMTP (Email Service)
BREVO_SMTP_HOST=smtp-relay.brevo.com
BREVO_SMTP_PORT=587
BREVO_SMTP_USER=your-email@example.com
BREVO_SMTP_PASS=xxxxxxxxxxxxxxxxxxx
MAIL_FROM=no-reply@salons-assured.com

# JWT Configuration (if needed for auth later)
JWT_SECRET=your_jwt_secret_here_at_least_32_chars
JWT_EXPIRES_IN=30d

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit .env file to git (already in .gitignore)
# 2. Use test keys for development, live keys for production
# 3. Rotate secrets every 90 days
# 4. For Render/Railway, set these in dashboard, not .env file

# ============================================
# HOW TO GET THESE VALUES
# ============================================
# MongoDB: mongodb.com/cloud/atlas (free M0 tier)
# Paystack: paystack.com → Settings → API Keys
# Brevo: brevo.com → Settings → SMTP & API (300 emails/day free)

# Test your config: cd backend && node test-security.js

